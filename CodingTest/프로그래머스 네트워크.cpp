#include <string>
#include <vector>

using namespace std;

int answer = 0;
vector<int> nodes[200];
bool isVisited[200];

int solution(int n, vector<vector<int>> computers) {
    for (size_t i = 0; i < computers.size(); i++)
    {
        for (size_t j = 0; j < computers[i].size(); i++)
        {
            
        }
    }
    
    return answer;
}